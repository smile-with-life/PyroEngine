function config()
    local config = {
        headers = {
            "Source/Core/Memory/**.h"
        },
        files = {
            "Source/Core/Memory/**.cpp"
        }
    }
    return config
end