function config()
    local config = {
        headers = {
            "Source/Core/Thread/*.h"
        },
        files = {
            "Source/Core/Thread/*.cpp"
        }
    }
    return config
end